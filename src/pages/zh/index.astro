---
import Layout from '@layouts/Layout.astro';
import { getCollection } from 'astro:content';

const posts = await getCollection('zh')
---

<Layout title="Index">
<h1>目录</h1>
<ul>
  {posts.map(post => <li>
    <a href={`/zh/posts/${post.slug}/`}>{post.data.title}</a>
    
</li>)}
</ul>
</Layout>