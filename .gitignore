node_modules
.DS_Store
/dist
.astro/

# Pi coding-agent sessions (persisted in-repo, like a docker volume)
/sessions/
/.pi/sessions/

# pnpm project — lockfiles from other package managers are not used
package-lock.json
yarn.lock