node_modules
.DS_Store
/.workbuddy/
/dist
.astro/

# Pi coding-agent workdir (sessions, subagents) — persisted in-repo
/sessions/
/.pi/

# Command Code agent state (taste, settings, file history)
/.commandcode/

# pnpm project — lockfiles from other package managers are not used
package-lock.json
yarn.lock