---
import PresentationLayout from "@components/PresentationLayout.astro";

// 数据与样式分离：每张卡片只声明内容，结构由下方统一渲染
const cards = [
  {
    anim: 0,
    cls: "premiseRow",
    bottomTitle: "张三借给李四 100 元,约定一年后归还",
    bottomClass: "premiseText",
    zhangEmoji: "😟",
    liEmoji: "😊",
    amount: "¥100",
    arrow: "→",
  },
  {
    anim: 1,
    cls: "resultCard resultDefault",
    bottomTitle: "坏结果 · 赖账",
    bottomClass: "resultTitleRed",
    zhangEmoji: "🤡",
    liEmoji: "🤣",
    amount: "¥0",
    arrow: "←",
  },
  {
    anim: 2,
    cls: "resultCard resultRepay",
    bottomTitle: "好结果 · 逾期还清本金",
    bottomClass: "resultTitleGreen",
    zhangEmoji: "😮‍💨",
    liEmoji: "🙂",
    amount: "¥100",
    arrow: "←",
  },
];
---

<PresentationLayout title="民间借贷的缺陷" eyebrow="债券" narrow>
  <div class="root">
    {cards.map((c) => (
      <div data-anim={c.anim} class:list={["card", c.cls]}>
        <div class="transfer">
          <div class="person">
            <div class="personEmoji">{c.zhangEmoji}</div>
            <div class="personName">张三</div>
          </div>
          <div class="transferAmount">
            <div class="amountNum">{c.amount}</div>
            <div class:list={["amountArrow", { reverse: c.arrow === "←" }]}></div>
          </div>
          <div class="person">
            <div class="personEmoji">{c.liEmoji}</div>
            <div class="personName">李四</div>
          </div>
        </div>
        <div class:list={["cardTitle", c.bottomClass]}>{c.bottomTitle}</div>
      </div>
    ))}
  </div>
</PresentationLayout>

<style>
  .root {
    height: 100%;
    display: grid;
    grid-template-columns: 1fr 1fr;
    grid-template-rows: 1fr 1fr;
    gap: 1rem;
  }

  /* Shared card base: column flex, uniform padding and centered text */
  .card {
    display: flex;
    flex-direction: column;
    gap: 1.2rem;
    padding: 1rem 1.4rem;
    text-align: center;
  }

  /* Shared bottom-title typography; color comes from the concrete class */
  .cardTitle {
    order: 2;
    font-size: 1.5rem;
    font-weight: 800;
  }

  /* Person diagram */
  .transfer {
    order: 1;
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 2.5rem;
  }

  .person {
    text-align: center;
  }

  .personEmoji {
    font-size: 4.4rem;
  }

  .personName {
    font-size: 1.3rem;
    font-weight: 700;
    color: #334155;
    margin-top: 0.2rem;
  }

  .transferAmount {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
  }

  .amountNum {
    font-size: 2.5rem;
    color: var(--amount, #b45309);
    font-weight: 800;
    line-height: 1;
  }

  /* CSS-drawn arrow so the arrow and the amount are visually the same height on every platform */
  .amountArrow {
    position: relative;
    width: 4.5rem;
    height: 0.4rem;
    background: var(--arrow, #f59e0b);
    border-radius: 0.2rem;
  }

  .amountArrow::after {
    content: "";
    position: absolute;
    right: -0.55rem;
    top: 50%;
    transform: translateY(-50%);
    border-left: 0.7rem solid var(--arrow, #f59e0b);
    border-top: 0.5rem solid transparent;
    border-bottom: 0.5rem solid transparent;
  }

  .amountArrow.reverse {
    transform: scaleX(-1);
  }

  /* Premise: spans the whole first column */
  .premiseRow {
    grid-column: 1;
    grid-row: 1 / -1;
    padding: 1rem 1.8rem;
    background: #fffdf4;
    border: 1px solid #e7e0cd;
    box-shadow: 0 2px 6px rgba(30, 41, 59, 0.04);
  }

  /* Result cards share the second column and a 2px border frame */
  .resultCard {
    grid-column: 2;
    border: 2px solid;
  }

  .resultDefault {
    grid-row: 1;
    justify-content: center;
    border-style: dashed;
    border-color: #fca5a5;
    background: #fef2f2;
    --amount: #b91c1c;
    --arrow: #ef4444;
  }

  .resultRepay {
    grid-row: 2;
    border-color: #86efac;
    background: #f0fdf4;
  }

  /* Title colors */
  .premiseText {
    color: #1e293b;
  }

  .resultTitleRed {
    color: #b91c1c;
  }

  .resultTitleGreen {
    color: #16a34a;
  }
</style>
